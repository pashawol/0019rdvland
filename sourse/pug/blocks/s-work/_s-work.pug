mixin s-work()
	// start s-work
	+b.SECTION.s-work.section.text-white.block-with-lazy#s-work&attributes(attributes)
		+pic('bg','work-bg',"lazy")(class="picture-bg")
		.container
			+b.section-title 
				h2 Как это работает?
			+e.slider-wrap
				+e.slider--js
					- for (var x = 0; x < 3; x++)
						+e.slide
							+pic('bg','slide',"lazy","jpg","440px")(class="picture-bg")
						// /+e.slide

	// end s-work